import React from 'react'
import SectionMain from '../../SectionMain'

const AppLayoutWrapper = ({ children }) => {
	return <SectionMain noPadding>{children}</SectionMain>
}

export default AppLayoutWrapper
